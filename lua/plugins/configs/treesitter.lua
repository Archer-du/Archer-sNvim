local options = {
  ensure_installed = { "lua", "c", "c_sharp", "cpp", "python", "rust", "cmake" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
